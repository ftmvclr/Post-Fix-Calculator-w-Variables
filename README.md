post-fix-calc.c takes an input file named input.txt which has the numericals and variable(chars) seperated by a white-space.
it searches and finds each possible permutation of solutions for variables.
keep in mind that variables can might as well be operators instead of simply just operands.
