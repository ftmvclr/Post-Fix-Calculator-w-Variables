post-fix-calc.c takes an input file named input.txt which has the numericals and variable(chars) each seperated by a white-space.  
It searches and finds each possible permutation of solutions for variables.  
Keep in mind that variables might as well be operators instead of simply just operands.  
